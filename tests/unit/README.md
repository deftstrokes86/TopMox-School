# Unit Tests

Fast unit tests live here.

Use this folder for pure TypeScript behavior:

- validation schemas
- service helpers
- status transition rules
- role helpers
- access-control helpers with mocked clients
- query filter/data-shaping helpers that do not need a database

Do not add tests here that require a running PostgreSQL database or browser.
