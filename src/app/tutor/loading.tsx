import { AppLoadingFallback } from "@/components/shared/AppLoadingFallback";

export default function TutorLoading() {
  return <AppLoadingFallback label="Preparing your TopMox tutor workspace..." />;
}
