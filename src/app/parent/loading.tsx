import { AppLoadingFallback } from "@/components/shared/AppLoadingFallback";

export default function ParentLoading() {
  return <AppLoadingFallback label="Preparing your TopMox parent workspace..." />;
}
